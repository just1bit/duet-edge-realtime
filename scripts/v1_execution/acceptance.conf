# Operational settings for the V1 acceptance workflow.
# Every path is relative to PROJECT_ROOT, the parent of duet-edge-realtime.

REALTIME_DIR="${REALTIME_DIR:-duet-edge-realtime}"
DUET_EDGE_DIR="${DUET_EDGE_DIR:-duet-edge}"
CHECKPOINT_PATH="${CHECKPOINT_PATH:-data+checkpoint/train-1800.pt}"
AIST_RAW_PATH="${AIST_RAW_PATH:-data+checkpoint/aist_plusplus_final/motions/gKR_sBM_cAll_d28_mKR2_ch06.pkl}"

CHECKPOINT_SHA256="2c948e74400ba78dbec469880746a78dfbb10ed56597917ba2e406cfeb8f9e15"
AIST_RAW_SHA256="54968522c7a41457a91d5c916e0f504db27c1afd696ca30bd2bc98dc12294e21"

PYTHON_BIN="${PYTHON_BIN:-python}"
ACCEPTANCE_PROFILE="${ACCEPTANCE_PROFILE:-gpu}"
HTTP_PORT="${HTTP_PORT:-8080}"
GPU_SAMPLE_INTERVAL_SECONDS="${GPU_SAMPLE_INTERVAL_SECONDS:-5}"
BASELINE_LOOPS="${BASELINE_LOOPS:-51}"
FINAL_LOOPS="${FINAL_LOOPS:-120}"
